#lang racket

;--------------;
;  question 3  ;
;--------------;
; Signature: deep-sum-digit(n)
; Type: 
; Purpose: 
; Pre-conditions: 
; Tests: 
 (define deep-sum-digit 
   (lambda (n)
     (...)))
 
 
;--------------;
;  question 4  ;
;--------------;
; Signature: 
; Type: 
; Purpose: Approximates 1/e as the sum of n first elements from n_i=(-1)^i/i!
; Pre-conditions: n>=0, integer 
; Tests: 
 (define calc-1-e
   (lambda (n)
     (...)))
 
;You can add functions if needed, 
;remeber to add a contract
;Note the order of the contract!!!
 
 
 