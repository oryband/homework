virtual void CsCourse :: reg(Student &s){

	// Check if student had finished its previous semester

	if (this->_semster == s._currentSemester &&
			s._unfinishedSemesterCourses == 0) {
		this->students.push_back(s);
	}
