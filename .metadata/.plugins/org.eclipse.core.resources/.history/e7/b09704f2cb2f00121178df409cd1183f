#include "../include/Department.h"

Department :: Department(std::string depName){
	this->_name.assign(depname);
}

void Department :: registerStudents(unsigned short semester){

	std::vector<Student::Student>::iterator student;
	vector<Course::Course>::iterator

	if (semerster == 1) { // Iterating Autumn Course list


	}
	if (semester == 0) { // Iterating Spring Course list

	}
}
